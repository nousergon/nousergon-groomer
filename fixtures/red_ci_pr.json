{
  "description": "An item in flight whose change has failing CI. Fixing CI is inside the loop's own write authority, so it is work (ACT), never a dependency (§3.5).",
  "items": [
    {
      "id": "p1",
      "stage": "in_flight",
      "title": "Add feature X",
      "labels": [],
      "change": {
        "ref": "p1",
        "condition": "ci_red",
        "mergeable": true,
        "ci_green": false,
        "security_threads": 0
      }
    }
  ],
  "world": {
    "s3_objects": [],
    "s3_prefixes": [],
    "terminal_items": [],
    "pipeline_runs": []
  },
  "expected": {
    "p1": {"kind": "act", "action": "fix_ci", "stage": "in_flight"}
  }
}
